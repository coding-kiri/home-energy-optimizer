# home-energy-optimizer
Project of a data pipeline for house hold energy usage optimization dashboard. A personal learning project about building E2E pipeline using AWS and Databricks.

Terraform (AWS S3, IAM, Databricks storage credentials) and dev/prod workflow: see [terraform/README.md](terraform/README.md). Databricks Asset Bundle targets: `databricks bundle deploy -t dev` or `-t prod`.
