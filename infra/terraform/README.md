# Terraform (AWS)

Terraform code for AWS resources lives here.

State backend, buckets, networking, and permissions are defined as part of Phase 0 in `docs/01_implementation_roadmap.md` (docs are intentionally not tracked yet).

