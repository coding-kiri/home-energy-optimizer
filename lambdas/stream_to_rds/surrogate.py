def household_key_from_household_id(household_id: str) -> int:
    """
    Placeholder for deterministic household key derivation.

    Implementation must exactly match the dbt surrogate key strategy used in
    gold.dim_household before deployment.
    """
    raise NotImplementedError("Implement deterministic household_id → household_key.")

